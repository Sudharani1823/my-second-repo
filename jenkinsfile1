pipeline {
    agent any

    environment {
        APP_NAME - "demoapp",
        VERSION = "1.0"
    }

    stages {
        stage('Build') {
            steps {
            echo "Building Application ${APP_NAME}"
            echo "version ${VERSION}"
            }
        }
    }
}