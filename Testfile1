pipeline {
    agent any
    environment {
        APP_NAME = 'my-app'
        ENV = 'dev'
}

    stages {
        stage('Build') {
            steps {
                echo 'Building APP_NAME'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing in ${ENV} environment'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }
    }
}