pipeline {
    agent any
    environment {
        'ENV' = 'prod'
    stages {
        stage('Build') {
            steps {
                echo "Building Application"
            }
            }
    
        stage('Test') {
            steps {
                echo "Testing Application"
            }
        }

        stage('Deploy') {
                when {
                    expression {
                        env.ENV == 'prod'
                    }
                }
                steps {
                echo "Deploying application"
            }
        }
    }
}