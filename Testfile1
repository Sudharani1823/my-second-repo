pipeline {
    agent any
    stages {
        stage('Build') {
            parameters{
                Choiice(
                    Name: Environment
                    choices: [dev,prod,qa]
                    Descripion: 'Select the Environment'
                )
            }
            steps {
                echo "Building ${APP_NAME}"
                echo "Environment is ${ENV}"
            }
            }
    
        stage('Test') {
            steps {
                echo "Testing Application"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying application"
            }
        }
    }
}