pipeline {
    agent any

}
    stages {
        stage('Build') {
            environment {
            APP_NAME = 'my-app'
            ENV = 'dev'
            }
            steps {
                echo "Building ${APP_NAME}"
                echo "Environment is ${ENV}"
            }
        }

        stage('Test') {
            steps {
                echo "Testing Application"
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploying application"
            }
        }
    }
}